# --- Configurações de Deploy (Digital Ocean / VPS) ---

# IP público ou Domínio do seu servidor
# Se estiver testando no PC local: http://localhost:8000/api/v1
# Se estiver na VPS: http://IP_DA_VPS:8000/api/v1
API_BASE_URL=http://localhost:8000/api/v1

# URL do Frontend (CORS) - de onde o navegador vai chamar a API
# Local: http://localhost:3000
# VPS: http://IP_DA_VPS:3000
ALLOWED_ORIGINS=http://localhost:3000

# --- Providers de IA ---

# Gemini (Obter chave em https://aistudio.google.com/)
GEMINI_API_KEY=sua_chave_aqui
DEFAULT_AI_PROVIDER=gemini

# Ollama (Opcional - Requer instalação no host)
ENABLE_OLLAMA=false
# Se o Ollama rodar no host (Linux), use host.docker.internal
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.2
